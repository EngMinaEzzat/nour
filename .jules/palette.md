## 2025-06-15 - Palette: Dark Glamour Theme Registration
**Learning:** When implementing complex UX logic changes inside config-heavy systems like `store-config.ts` involving conditional variables (like `isDarkGlamour` combined with translation layers via `t()`), carefully verify that string constants align and all variants of the conditional checks accurately map to the translation JSON structure (including keys like `lookbook`).
**Action:** Always test rendering properties like nested string arrays logic and `aria-label` internationalizations early in the implementation stage to ensure no UI breakages or unexpected `undefined` labels on accessible elements.
## 2025-06-15 - Palette: Dark Glamour Theme Registration
**Learning:** When generating placeholder or fallback images locally via the filesystem during UX changes, always ensure they are named exactly as referenced by the source code (`-glamour-optimized.jpg`), since mistyped paths result in hard-to-track visual bugs, especially when using dynamic rendering layers that rely on them.
**Action:** Use programmatic copies (like `cp src-name dest-name`) to quickly map fallback assets. Verify image resolution paths with the actual source code logic mapped via components like `Hero` and `About` in the design config.
