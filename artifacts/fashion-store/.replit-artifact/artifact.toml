kind = "web"
previewPath = "/"
title = "Nour — Egyptian Fashion & Beauty Marketplace"
version = "1.0.0"
id = "artifacts/fashion-store"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/fashion-store run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fashion-store", "run", "build" ]
publicDir = "artifacts/fashion-store/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
